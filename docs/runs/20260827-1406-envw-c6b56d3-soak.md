# Run report — soak parziale ~7,4 h su stack full (baseline "prima" di P-5)

- **RUN_ID**: `20260827-1406-envw-c6b56d3`
- **Ambiente**: ENV-W (HP Z8 G4, 2× Xeon 6244, 256 GB RAM, RTX 3090 24 GB, Ubuntu 24)
- **Harness**: `bench/soak/` @ `c6b56d3` (branch `feature/soak-harness`)
- **Stack**: `wap-northstream-lab` @ `76dd69c`, profilo **full**, 19 container, generatore attivo
- **Finestra**: 2026-08-27T14:06:35Z → 2026-08-27T21:29:44Z — **26 589 s = 7,386 h**
- **Campioni**: **427**, `seq` 0…426 contigui, intervallo nominale 60 s (medio reale 62,4 s)
- **Esclusività**: `exclusive` — dichiarata dall'operatore (vedi §5)
- **Grezzi**: `~/NORTHSTREAM-archive/20260827-1406-envw-c6b56d3/` con `SHA256SUMS` verificato

## 0. Condizioni in cui il dato è stato raccolto e analizzato

Questo report va letto sapendo tre cose, perché nessuna delle tre è deducibile dai numeri:

1. **È un parziale, non T-SOAK-24h.** La durata prescritta dal piano per T-SOAK-24h è 24 h;
   qui sono 7,4 h, perché tanto restava della finestra di manutenzione dichiarata dall'owner
   il 27/08. Il punto (a) delle verifiche §4.3 — crescita Qdrant coerente con la retention —
   **non è chiudibile su 7 h**: una retention non ancora implementata (A-3) non può essere
   distinta da una retention con finestra più lunga della misura.
2. **L'analisi è del giorno dopo.** Le sessioni si sono fermate la sera del 27/08 per
   esaurimento dei crediti settimanali; il verdetto, l'archiviazione e questo report sono
   stati prodotti il **28/08 verso le 12:10Z**, ~15 h dopo la fine del run.
3. **Il campionatore è sopravvissuto alla caduta della sessione perché era distaccato**
   (PPID 1) e il formato è append-only con `flush`+`fsync` per riga. Il run ha raggiunto la
   sua fine prevista (`planned_end` 21:30:00Z, ultimo campione 21:29:44Z): **non è troncato.**
   Integrità verificata a posteriori: 427/427 righe JSON valide, `seq` contigui 0…426,
   `soak.err.log` a **0 byte** (SHA256 `e3b0c442…852b855`, che è l'hash del file vuoto —
   conferma indipendente, non solo `wc -c`).

## 1. Verdetto sulle quattro verifiche §4.3

Output di `bench/soak/verdict.py --samples … --manifest …`, integrale:

```
soak verdict — 20260827-1406-envw-c6b56d3 — 427 sample(s), 2026-08-27T14:06:35Z .. 2026-08-27T21:29:44Z

  WARN     qdrant_growth_vs_retention
           points 1943 -> 10296 (+8353, 1130.9/h); monotonically increasing across the whole
           run — no retention observed, matches A-3 until it is fixed in v0.0.4; no ceiling
           declared to fail against
  UNKNOWN  replication_slot_size
           peak retained WAL 0.1 MiB at 2026-08-27T15:26:41Z — no --max-replication-mib
           supplied, reporting trend only
  OK       event_loss_db_vs_qdrant
           db rows +8352 (2038->10390), qdrant points +8353 (1943->10296)
  UNKNOWN  rss_vs_tier
           peak total RSS 15985 MiB across 19 container(s) — no --rss-ceiling-mib supplied
```

**Le due `UNKNOWN` non sono un'omissione dell'operatore.** Il piano non dichiara né una
soglia per lo slot di replica né un tetto RSS per lo stack full: l'unico tetto scritto è
quello del profilo `core` (≤ 14 GB, T-PROF, v0.0.3, e per giunta **su ENV-L**). Passare una
soglia inventata da riga di comando avrebbe prodotto un `OK`/`WARN` senza referente nel piano.
Restano `UNKNOWN` finché una soglia non è dichiarata: è il verdetto corretto, non un buco.

### (a) Crescita Qdrant — `WARN`, atteso
1943 → 10 296 punti, +8353 in 7,4 h, **1131 punti/h, monotòna su tutto il run**. Nessuna
retention osservata, come previsto da A-3 (aperto fino a v0.0.4). Vedi §0.1: su 7 h questo
`WARN` è compatibile sia con "nessuna retention" sia con "retention a finestra > 7 h"; è la
diagnosi già nota di A-3 a farlo pendere sulla prima, non questa misura.

### (b) Slot di replica — `UNKNOWN`, ma la tendenza è piatta e va detta
WAL trattenuto **min 2 712 B, max 63 152 B = 0,060 MiB**, picco alle 15:26:41Z. Su 7,4 h di
generatore attivo lo slot **non accumula**: il consumatore Debezium sta al passo. Senza soglia
dichiarata il verdetto resta `UNKNOWN`, ma il numero è quello di uno slot sano, non di uno che
cresce lentamente.

**Lo slot risulta `active: true` in 427 campioni su 427.** Vale la pena dirlo perché il
27/08 il campionatore riportava `inactive` in ogni campione: era un difetto di parsing
(`active == "t"` contro un Postgres che rende il booleano come `true`), corretto in `2b4f4ad`
e verificato prima di questo run con 5 sonde contro `psql` (attivo / connettore in pausa /
ripreso / `debezium-connect` fermo / riavviato: sempre concordi). Nessun campione porta
`warnings` non vuoti, e nessuno porta `postgres.error` o `qdrant.error`: **427 letture pulite
su 427.**

### (c) Eventi persi — `OK`
DB 2 038 → 10 390 righe (`orders` 1031→5222, `sensor_readings` 1007→5168); Qdrant 1943 →
10 296. Delta: **DB +8 352, Qdrant +8 353.**

Il divario assoluto fra i due contatori è **94 al minimo, 94 in mediana, 96 al massimo**, su
427 campioni: costante entro l'errore di campionamento. Le due letture non sono atomiche —
`psql` e l'API Qdrant sono interrogati in sequenza, con la pipeline che nel frattempo lavora —
quindi un divario di ±1 attorno a un valore fisso è rumore, non perdita. Che il delta di
Qdrant superi di **uno** quello del DB nell'arco del run è la stessa cosa vista dall'altro lato.
Il divario costante di ~95 è il backlog residuo antecedente al run, non un tasso di perdita:
se si perdessero eventi il divario **crescerebbe**, e su 7,4 h non cresce.

### (d) RSS totale per profilo — `UNKNOWN` per soglia mancante, ma è **la misura "prima" di P-5**

È il risultato di maggior valore del run, ed è stato ottenuto a costo zero da campioni
raccolti per altro.

| | MiB | GiB |
|---|---|---|
| minimo (primo campione, a freddo) | 12 376 | 12,09 |
| **mediana** | **15 174** | **14,82** |
| massimo | 15 985 | 15,61 |
| ultimo campione | 15 947 | 15,57 |

Ripartizione per servizio (mediana su 427 campioni, primi 8, MiB):

| servizio | mediana | primo campione | ultimo campione |
|---|---:|---:|---:|
| **trino** | **6 517** | **4 109** | **7 119** |
| elasticsearch | 1 719 | 1 712 | 1 719 |
| schema-registry | 1 217 | 993 | 1 228 |
| openmetadata | 1 013 | 1 010 | 1 014 |
| kafka | 970 | 960 | 978 |
| open-webui | 687 | 687 | 687 |
| ollama | 649 | 649 | 650 |
| debezium-connect | 574 | 499 | 573 |

**Trino cresce da 4 109 a 7 119 MiB in 7,4 ore** — +73%, e da solo spiega quasi tutta la
crescita dello stack (+3 571 MiB totali fra primo e ultimo campione, di cui 3 010 sono Trino).
Elasticsearch (1 712→1 719, l'unico con `-Xmx` fisso nel compose) e Kafka (960→978) sono
piatti. È esattamente il quadro di P-5: **una JVM senza tetto su una macchina con 256 GB
dimensiona l'heap su una frazione della RAM di sistema e poi lo riempie.**

**Avvertenza vincolante, dal piano §143**: *"Le misure di RAM qui non sono trasferibili ai
tier"* — con 256 GB fisici il footprint osservato su ENV-W è un **limite superiore**, e P-5 e
T-PROF si validano solo su ENV-L o con `mem_limit` espliciti. Questi numeri servono come
termine di paragone **"prima"** contro un "dopo" misurato **sulla stessa macchina con la stessa
durata**, non come valore di tier.

## 2. Annotazione al run di riferimento del 26/08

Questa misura corregge la lettura di un dato già archiviato, e la correzione va annotata dove
sta il dato. `docs/runs/20260826-2053-envw-5eb456a-baseline.md` riporta **9,52 GiB** di RSS
totale per lo stack full. Quel numero **non è sbagliato: è una lettura a freddo di una
grandezza che cresce.** Il primo campione di questo soak, anch'esso a freddo, dà 12,09 GiB; la
mediana a regime dà 14,82 GiB. Il report del 26/08 è stato **annotato, non riscritto**: i suoi
esiti di test restano validi, cambia solo cosa si può concludere da quella cifra.

## 3. Esclusività dell'host (#44, descrittivo)

```
gpu0: min 362 MiB, max 2 671 MiB su 24 576, 427 campioni
load1: min 0,60 · mediana 2,71 · max 30,90
RAM disponibile: 213 934 – 219 653 MiB su 241 504
```

VRAM: **mediana e p95 entrambi 362 MiB**; solo **5 campioni su 427 (1,2%)** superano 1 GiB, e
il picco di 2 671 MiB è attribuibile al nostro `llama-server`. Nessun processo di calcolo
estraneo. Questo conferma per misura ciò che era dichiarato: la macchina è stata **esclusiva**
per tutta la durata.

Sul significato dei 362 MiB — la domanda posta il 27/08 ("l'agent ha smesso di indicizzare?") —
la risposta è **fisiologico**: nello stesso arco di tempo Qdrant è cresciuto di 8 353 punti a
ritmo costante, quindi l'indicizzazione stava avvenendo. L'ingest usa il modello di embedding
piccolo; il footprint grosso (~6,5 GiB) appare solo con la chat attiva, e 19,8 GiB solo con
`granite4:32b-a9b-h` in EVAL.

## 4. Cosa questo run **non** dice

- Non chiude T-SOAK-24h: è 7,4 h su 24 (§0.1).
- Non valida i tier: ENV-W ha 256 GB, le JVM non hanno tetto, i numeri sono un limite
  superiore (§1d).
- Non dice **quale** delle due parti dell'intervento su P-5 funzionerà: `mem_limit` (#21) e
  `jvm.config` (#22) cambiano insieme, quindi un "dopo" piatto proverà l'intervento
  **combinato**, non l'una o l'altra metà.
- Non copre il secondo soak ("dopo"), **mai lanciato**: la finestra esclusiva è scaduta alle
  ~08:15Z del 28/08 e va richiesta di nuovo all'owner.

## 5. Nota di metodo sul `manifest.json`

Il manifest di questo run contiene un blocco `operator_recorded` **aggiunto a mano**, ed è
etichettato come tale: al momento del lancio `run.sh` scriveva `"exclusivity": "unknown"`
cablato e non raccoglieva lo stato iniziale di connettore e slot. Il flag `--exclusivity` e la
raccolta di `initial_conditions` sono arrivati dopo, in `fe91a74`: **dal prossimo run i campi
sono nativi e questa annotazione manuale non va più fatta.** Il campo `declared` e il campo
`detected` vanno tenuti affiancati e non fusi.

## 6. Costo della sessione

**Non misurabile** (sessione bridge).
