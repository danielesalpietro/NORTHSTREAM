# Run report — T0.7, verifica di #44 a tre stati, suite T0 completa su `release/v0.0.3`

- **RUN_ID**: `20260828-1247-envw-9e71cd5`
- **Ambiente**: ENV-W (HP Z8 G4, RTX 3090 24 GB), finestra di manutenzione esclusiva
- **Repo**: `release/v0.0.3` @ `9e71cd5`, clone `/home/admin/claude/ns-work`
- **Stack**: ricreato da zero alle **12:31:43Z → 12:34:35Z**, profili `core,lakehouse,governance`
  (19 container), `--gpu`
- **Grezzi**: `~/NORTHSTREAM-archive/20260828-1247-envw-9e71cd5/` con `SHA256SUMS` verificato,
  incluse le evidenze di #44 e la controprova del difetto di T0.7

## 0. Precondizioni verificate prima di misurare

Lo stack in esecuzione era partito il 27/08 alle 14:06, **prima** dei commit di configurazione
di v0.0.3: era il vecchio Trino, senza `jvm.config` né `mem_limit`. È stato distrutto e
ricreato. **Teardown senza `-v`**: gli otto volumi di stato, `ollama_data` compreso,
sopravvivono — verificato con `docker volume ls` subito dopo (lezione di P-10/#42).

Tre controlli fatti *prima* di prendere qualunque numero, perché ciascuno di essi ha già
prodotto una misura falsa in passato:

```
docker inspect northstream-ollama --format '{{json .HostConfig.DeviceRequests}}'
  → [{"Driver":"nvidia","Count":-1,...,"Capabilities":[["gpu"]]}]     (non null: GPU passata)
docker exec northstream-trino cat /etc/trino/jvm.config | grep '^-Xm'
  → -Xms1G / -Xmx2G                                    (la config di #22 è davvero montata)
docker exec northstream-trino ls /etc/trino/catalog
  → postgresql.properties                              (il catalogo di #23 è davvero montato)
```

## 1. T0.7 — PASS nella sostanza, **ma il test ha un difetto che va corretto**

### 1.1 L'esito
```
--- T0.7 (t0.07_trino_catalog.sh) — expected: PASS
    PASS   OK     (3s) Trino queried postgresql.public.orders (282026124836 rows)
```

### 1.2 Il difetto: `282026124836` non è un conteggio
È la data della riga di WARNING di jline (`Aug 28, 2026 12:48:36 PM`) con i non-cifre rimossi.
L'output grezzo di Trino è di tre righe, e il conteggio è la terza:

```
Aug 28, 2026 12:35:10 PM org.jline.utils.Log logr
WARNING: Unable to create a system terminal, creating a dumb terminal (...)
"13748"
```

Il test fa `head -1` — che prende la riga di WARNING — e poi `tr -dc '0-9'`.
**La riga del conteggio non viene mai letta.**

**Controprova decisiva**, una query che restituisce zero per costruzione:

```
$ trino --execute "SELECT count(*) FROM postgresql.public.orders WHERE 1=0"
Aug 28, 2026 12:35:12 PM org.jline.utils.Log logr
WARNING: ...
"0"
count estratto dal test -> 282026123512        (> 0 ⇒ il test direbbe OK)
```

**Il test passerebbe con un conteggio di zero.** La seconda asserzione di T0.7 non discrimina.
La prima (`grep '"postgresql"'` sull'output di `SHOW CATALOGS`) è invece reale e discrimina.

### 1.3 La sostanza, misurata a mano
```
psql -U demo -d sales -c 'SELECT count(*) FROM public.orders'      → 13754
trino --execute 'SELECT count(*) FROM postgresql.public.orders'    → 13755
```
Uno di scarto, generatore attivo, letture non atomiche. **Trino legge davvero la sorgente
operativa: la metà Trino di P-2 è chiusa.**

### 1.4 Conseguenza sull'attesa
`bench/t0/expected/current.json` è stato aggiornato: `T0.7: XFAIL → PASS`, e la voce
corrispondente rimossa da `flips`. **Il flip poggia sulla misura manuale di §1.3, non
sull'asserzione difettosa del test** — e questo va detto, perché altrimenti fra sei mesi il
verde di T0.7 sembrerà una prova che non è.

> **Difetto da aprire come issue** (non lo correggo io: sono host di verifica, non committo
> codice di test). La correzione è di una riga — prendere l'ultima riga invece della prima, e
> pretendere che sia della forma `"<cifre>"` — ma è un cambiamento al codice di test e va fatto
> da chi possiede `bench/`.

## 2. Verifica di #44 — tre stati osservati, più il caso non misurabile

Protocollo identico a quello usato per P-11: **uno stato solo non è una verifica.**
Evidenze integrali in `44-esclusivita-gpu-evidenze.txt` nell'archivio.

**Precondizione voluta**: Ollama è stato scaldato di proposito (`granite4:1b` +
`granite-embedding:30m` residenti, due processi di calcolo reali sulla GPU), così che lo stato
A verifichi l'**attribuzione** e non il caso banale a zero processi. Entrambi i pid risolvono
allo stesso cgroup `0478c8bd…f3115f3cc` = `northstream-ollama`.

| stato | condizione | `state` | dettaglio |
|---|---|---|---|
| **A** libera | 2 processi, entrambi nostri | **`exclusive`** | `foreign_process_count: 0`, 6 489/24 576 MiB |
| **B** contesa | + 1 container CUDA **senza** il label di progetto, 1,5 GiB allocati | **`shared`** | `foreign_process_count: 1`, `foreign_used_mib: 1896` |
| — | `nvidia-smi` assente, **contesa ancora attiva** | **`unknown`** | tutti i campi `null`, *"not applicable on this host, not evidence of contention"* |
| **C** risanata | tenant fermato | **`exclusive`** | il terzo pid sparisce, `foreign_used_mib: 0` |

**Il caso `unknown` è quello che conta di più**, ed è stato provato nel modo scomodo: con la
contesa *realmente in corso*. Un modulo che collassasse "non lo so" su "condiviso" avrebbe dato
la risposta giusta per caso. Questo dice `unknown` e non `shared`, che è la regola CLAUDE.md §5
sul booleano che non deve fondere "falso" e "non lo so".

> **Auto-correzione, registrata perché è il tipo di errore che produce misure false.** Il primo
> tentativo di nascondere `nvidia-smi` è stato `env PATH=/usr/bin:/bin` — ma `nvidia-smi` **sta**
> in `/usr/bin`, quindi non era nascosto affatto. Il modulo rispose `shared`, che era la risposta
> giusta a una domanda che non avevo posto. Rifatto con un PATH sandbox di soli symlink. Me ne
> sono accorto solo perché avevo stampato `nvidia-smi visibile? -> …` accanto al risultato.

**Comportamento del cancello**, sotto la stessa contesa reale:

| | comando | esito | exit |
|---|---|---|---|
| B1 | `--gpu --require-vram-mib 20000` | **FAIL**, "…leaving 16186 MiB free … will not fit" | 1 |
| B2 | `… 20000 --allow-contention` | **WARN**, "Starting anyway" | 0 |
| B3 | `--gpu --require-vram-mib 8000` | **WARN**, "free VRAM still covers the 8000 MiB declared" | 0 |
| B4 | `--gpu` (nessuna soglia) | **WARN**, "no `--require-vram-mib` declared, reporting only" | 0 |

Con `nvidia-smi` assente e soglia dichiarata, il controllo di esclusività dice **WARN — "could
not be determined … Not treated as contention, only as an unmeasured host"**. Il comando esce
comunque 1, ma per un controllo **diverso e corretto** (`--gpu` richiesto senza driver
disponibile): i due sono distinti nell'output e non vanno confusi.

**#44 è verificato lato hardware.** `declared` (dal flag) e `detected` (dal modulo) restano
campi separati e non fusi.

## 3. Suite T0 completa — nessuna regressione

Due esecuzioni consecutive, 12:40Z e 12:47Z, sullo stesso stack. La prima è archiviata come
`t0-run-precedente-envx/`: porta il tag `envx` perché avevo omesso `NS_ENV`, che è il default
documentato, non un difetto. La seconda è quella canonica.

```
counts:  {"PASS": 9, "XFAIL": 3, "XPASS": 1}
RESULT:  OK (no regression)
```

9 PASS (T0.1–T0.7, T0.12, T-REPRO), 3 XFAIL attesi (T0.8/A-3, T0.10/A-1, T0.11/A-5),
1 XPASS (T0.9).

### 3.1 Il reperto: T0.10 cambia esito fra due run a sette minuti di distanza

| | run 12:40Z | run 12:47Z |
|---|---|---|
| T0.10 | **XPASS** — "retrieval surfaced the Depot-9 event" | **XFAIL** — "retrieval missed the Depot-9 event" |

Stesso stack, stessa SHA, sette minuti. **È l'issue #40 colta in flagrante**: T0.10 non è
robusto alla dimensione del corpus, e il suo esito è una funzione di quanti punti ci sono in
Qdrant al momento in cui gira, non del difetto A-1 che dovrebbe misurare. Un XPASS di T0.10
**non va mai letto come "A-1 risolto"**. Questa è la prima osservazione *diretta* della
non-determinatezza: prima era un'inferenza dal run di riferimento.

### 3.2 T0.9 in XPASS, stabile su due osservazioni — **da analizzare, non da promuovere**

T0.9 ("nessun evento più vecchio di 300 s nel contesto o nella risposta", atteso XFAIL fino a
v0.0.4 per A-2) dà XPASS in **entrambi** i run, con durate quasi identiche (323 s e 321 s).
Due osservazioni concordi sono più di quanto avessi per T0.10, ma **non bastano a dichiarare
A-2 risolto**: resta da stabilire se il test discrimini davvero in queste condizioni, o se lo
stato del corpus lo renda vero per costruzione. **Non tocco l'attesa di T0.9**: è una decisione
di analisi, non un'esecuzione, e non è mia. La lascio come reperto.

## 4. Risposta alla domanda dell'owner sulla VRAM

Dai 427 campioni già archiviati del soak `20260827-1406-envw-c6b56d3` (7,4 h, stack pieno,
ingest continuo, **nessuna sessione di chat**):

| | MiB |
|---|---|
| min | 362 |
| **mediana** | **362** |
| p90 | 362 |
| p95 | 362 |
| p99 | 1 649 |
| media | 380,7 |
| max | 2 671 |

| soglia | campioni sopra | % |
|---|---|---|
| 512 MiB | 7 / 427 | 1,64 % |
| 1 024 MiB | 5 / 427 | 1,17 % |
| 2 048 MiB | 1 / 427 | 0,23 % |

Tempo cumulato sopra 1 GiB: **~5,2 minuti su 7,4 ore.** I soli valori distinti sopra 1 GiB sono
1 649, 1 651 e 2 671 MiB.

**Il verdetto è "footprint stazionario ~362 MiB con picchi brevi e rari", non "6,5 GiB
sostenuti"** — ma con tre avvertenze che cambiano la conclusione:

1. Durante quel soak **non c'è stata attività di chat**. Il footprint misurato è quello del
   solo *ingest*, cioè il modello di embedding.
2. Con la chat attiva la coppia `granite4:1b` + `granite-embedding:30m` occupa **6 489 MiB** —
   misurato oggi, stato A di §2.
3. `granite4:32b-a9b-h` in EVAL ne occupa **19 788 MiB**, misurato il 27/08.

Quindi: per una macchina di solo ingest basterebbe una scheda molto più piccola; per la
demo con chat servono ~8 GB di VRAM; per tenere il 32b in matrice EVAL servono i 24 GB.
**La scelta dipende da quale dei tre usi si vuole coprire, e i tre numeri sono lontanissimi
fra loro** — non è una domanda a cui un solo numero risponda.

## 5. Costo della sessione

**Non misurabile** (sessione bridge).
