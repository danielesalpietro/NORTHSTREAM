# Run report — soak #2, il "dopo" di P-5: lo stack dimezzato e Trino che si assesta

- **RUN_ID**: `20260828-1342-envw-7f9e5a9`
- **Ambiente**: ENV-W, finestra esclusiva (`state: exclusive`, verificato con `gpu_exclusivity.py`)
- **Stack**: pieno, **tutti e tre i profili**, 19 container, da `release/v0.0.3` @ `7f9e5a9`
- **Harness**: `bench/soak/` @ `c83bdff`, `--exclusivity exclusive` e `initial_conditions` **nativi**
- **Finestra**: 2026-08-28T13:42:43Z → 21:05:32Z — **26 569 s = 7,380 h**
- **Campioni**: **420**, `seq` 0…419 contigui, 0 righe malformate, 12 con `containers._error`
- **Intervallo**: nominale 60 s, **reale medio 63,4 s** (soak #1: 62,4 s)
- **Grezzi**: `~/NORTHSTREAM-archive/20260828-1342-envw-7f9e5a9/`, `SHA256SUMS` verificato

## 0. Condizioni della misura, dichiarate prima dei numeri

1. **La finestra comune con il soak #1 è 26 569 s = 7,380 h**, cioè *praticamente entrambi i
   run interi*: il soak #1 dura 26 589 s, venti secondi in più. Tutti i confronti di §2 sono
   calcolati su questa finestra, troncando il soak #1 al suo 426° campione.
2. **Unica variabile dichiarata**: la configurazione di #21 (`mem_limit`) + #22 (`jvm.config`).
   Stack, composizione, generatore, cadenza e durata sono gli stessi. **`mem_limit` e
   `jvm.config` cambiano insieme**, quindi ciò che segue prova l'intervento **combinato**, non
   quale delle due metà lo produca.
3. **`open-webui` è contaminato e i suoi numeri non vanno usati**: nello stack di questo run il
   suo `mem_limit` era ancora 512 MiB e il servizio ha fatto **3 474 riavvii**. Il fix
   `f9a56fc` è arrivato a soak già avviato e **lo stack non è stato riavviato di proposito**:
   il soak valeva più della correzione, che entra al prossimo avvio.
4. **L'analisi è del giorno dopo.** Il run ha raggiunto la sua fine prevista (ultimo campione
   21:05:32Z contro un `planned_end` di 21:06:12Z), ma la sessione è caduta e verdetto,
   archiviazione e report sono del **29/08 verso le 06:00Z**. Il campionatore era distaccato e
   i dati hanno aspettato su disco, come il 27/08.

## 1. Il risultato: lo stack è dimezzato

| | PRIMA (soak #1, senza tetti) | DOPO (soak #2, con i tetti) |
|---|---:|---:|
| totale, primo campione | 12 376 MiB | **6 982 MiB** |
| **totale, mediana** | **15 170 MiB** | **7 986 MiB** |
| totale, ultimo campione | 15 943 MiB | 8 158 MiB |
| totale, massimo | 15 985 MiB | 8 490 MiB |
| al netto di `ollama` (esente), mediana | 14 521 MiB | **7 301 MiB** |
| `ollama` (esente, riportato a parte) | mediana 649, max 650 MiB | mediana 686, max 688 MiB |

**Mediana 15 170 → 7 986 MiB: −47 %.** Il tetto di §4.3.1(d) per il profilo pieno è
**16 768 MiB** (somma dei `mem_limit`, `ollama` escluso per esenzione dichiarata): il "dopo"
ci sta dentro con il **44 %** del budget, il "prima" ci stava per un soffio (14 521) pur non
avendo alcun tetto da rispettare.

### 1.1 Trino — la risposta a P-5, e non è il valore assoluto, è la pendenza

| | PRIMA | DOPO |
|---|---:|---:|
| primo campione | 4 109 MiB | **981 MiB** |
| ultimo campione | 7 117 MiB | **1 572 MiB** |
| crescita sul run | **+3 007 MiB (+73 %)** | **+591 MiB (+60 %)** |

La crescita *relativa* è simile, e presa da sola direbbe che non è cambiato molto. **È la
domanda sbagliata.** Quella giusta è se la curva si assesta, e la risposta è nella pendenza per
quarto di run:

| pendenza (MiB/h) | Q1 | Q2 | Q3 | Q4 | seconda metà |
|---|---:|---:|---:|---:|---:|
| **PRIMA**, senza tetti | +694,4 | +629,9 | +183,1 | **+129,7** | **+150,0** |
| **DOPO**, con i tetti | +168,7 | +164,7 | +0,5 | **+0,3** | **+0,5** |

**Senza tetti, dopo 7,4 ore Trino saliva ancora a 130 MiB/h e non accennava a fermarsi. Con i
tetti si assesta entro metà run e poi è piatto: +0,5 MiB/h sulla seconda metà, trecento volte
meno.** Estrapolando la pendenza del "prima" — con la cautela che un'estrapolazione merita —
un run di 24 ore avrebbe portato Trino oltre i 9 GiB. Con i tetti resta a 1 572 MiB, il 61 %
del proprio `mem_limit` di 2 560.

**P-5 è chiuso nel comportamento**, e il margine non è discutibile.

### 1.2 Gli altri servizi, sulla finestra comune

| servizio | #1 primo → ultimo | #2 primo → ultimo |
|---|---|---|
| **trino** | 4 109 → 7 117 (**+3 007**) | 981 → 1 572 (**+591**) |
| **schema-registry** | 993 → 1 228 (**+235**) | 220 → 240 (**+20**) |
| kafka-ui | 529 → 565 (+36) | 204 → 255 (+50) |
| kafka | 960 → 978 (+17) | 562 → 702 (+140) |
| debezium-connect | 499 → 573 (+74) | 566 → 608 (+42) |
| elasticsearch | 1 712 → 1 719 (+7) | 1 526 → 1 502 (−24) ← vedi §3 |
| openmetadata | 1 010 → 1 014 (+4) | 1 039 → 1 013 (−26) |
| flink-jobmanager | 342 → 405 (+63) | 269 → 447 (+178) |
| flink-taskmanager | 410 → 507 (+96) | 371 → 442 (+70) |
| ~~open-webui~~ | ~~687 → 687~~ | ~~144 → 188~~ **contaminato, §0.3** |
| qdrant · minio · postgres · adminer · data-generator · landing · openmetadata-db · stream-agent | piatti | piatti |

`schema-registry` è il secondo guadagno per dimensione dopo Trino: **da +235 a +20 MiB** di
crescita. `kafka` e `flink-jobmanager` crescono *di più* in assoluto nel "dopo", ma partendo da
molto più in basso e restando abbondantemente sotto il proprio tetto.

## 2. Verdetto sulle quattro verifiche §4.3

Applicate come §4.3.1 è scritta **adesso**, non come `verdict.py` la implementa. Dove le due
divergono, lo dico.

### (a) Crescita Qdrant → **FAIL**

```
soak #2: qdrant 27 414 → 27 414 (+0)   mentre   db 28 471 → 36 747 (+8 276)
```

È la riga *"conteggio piatto o calante **mentre** gli eventi nel DB crescono"* di §4.3.1(a):
**FAIL**, il caso più grave. La causa è **A-3**, osservata al livello del singolo punto durante
il run: l'agent ha rimesso a zero il contatore di `point_id` al riavvio del container e sta
riscrivendo sopra il corpus esistente — `id=3` porta un evento delle 13:41:55Z del 28/08,
`id=27413` uno delle 12:31Z dello stesso giorno. Evidenze in
`a3-sovrascrittura-in-produzione.txt`.

> **`verdict.py` qui dice `OK`.** Testualmente: *"points 27414 -> 27414 (+0, 0.0/h); growth
> plateaued or dropped at least once — bounded"*. Il conteggio è piatto **perché i dati vengono
> distrutti**, e il check lo legge come crescita limitata. È il difetto già documentato in
> `20260827-1406-envw-c6b56d3-soak.md` §8, e questo run è la sua dimostrazione dal vivo:
> **il verdetto automatico e il verdetto secondo il piano sono opposti, e ha ragione il piano.**

### (b) Replication slot → **OK**, tutte e quattro le condizioni

| condizione | richiesto | soak #1 | soak #2 |
|---|---|---:|---:|
| campioni con `active` null | 0 | 0 | **0** |
| frazione `active = true` | ≥ 99 % | 100,00 % | **100,00 %** |
| sequenza max non-attiva | < 3 | 0 | **0** |
| WAL trattenuto max | ≤ 256 MiB | 0,0602 | **0,1217** |
| pendenza seconda metà | ≤ 1 MiB/h | −0,000456 | **+0,000143** |

Lo slot regge in entrambe le configurazioni. Il WAL massimo raddoppia (0,060 → 0,122 MiB) ma
resta tre ordini di grandezza sotto il tetto, e la pendenza è indistinguibile da zero.

### (c) Eventi persi → **FAIL**

```
soak #1: db +8 352, qdrant +8 353,  divario 95 → 94   (min 94, max 96)  — costante
soak #2: db +8 276, qdrant     +0,  divario 1 057 → 9 333               — cresce senza sosta
```

Il divario **cresce monotonicamente**, che è la firma della perdita. Ma la perdita **non è in
transito**: è A-3 che sovrascrive a valle. Il conteggio dei punti ha smesso di essere un
indicatore di quanti eventi sono stati indicizzati, ed è (c) — non (a) — ad averlo scoperto.

### (d) RSS totale → **FAIL**, e non per il totale

**Il totale passa:** 7 301 MiB al netto di `ollama` contro un tetto di 16 768 MiB, il 44 %.
`ollama` è **esente per esenzione dichiarata nel compose** e riportato a parte: mediana 686
MiB, massimo 688.

**Fallisce la seconda condizione**, quella per servizio:

| servizio | tetto | mediana | % | campioni > 90 % | sequenza consecutiva max |
|---|---:|---:|---:|---:|---:|
| **elasticsearch** | 1 536 | 1 529 | **99,5 %** | **404 / 408** | **356** (la regola chiede ≥ 10) |
| open-webui | 512 | 138 | 27,0 % | 61 / 408 | 2 |

Dettaglio completo in `docs/runs/20260828-1819-envw-7f9e5a9-sweep.md`: l'aritmetica di ES è
chiusa — `-Xmx1g` + `MaxDirectMemorySize 512m` = **esattamente** il `mem_limit` di 1 536 MiB,
zero byte per metaspace, code cache, stack e native. `memory.peak` legge 1 536,0 MiB esatti,
`RestartCount` è passato da **7 alle 18:19Z a 23 alle 05:56Z**.

**Un verdetto sul solo totale avrebbe dato OK.** La seconda condizione di §4.3.1(d), alla sua
prima applicazione reale, ha trovato ciò per cui era stata scritta.

## 3. Due avvertenze su numeri che sembrano buoni

**`elasticsearch` scende da 1 712 a 1 502 MiB fra i due run.** Sembra un guadagno dei tetti.
**Non lo è**: ES sbatte contro il proprio tetto e riparte, 23 volte. Il numero più basso è il
sintomo, non il risultato — la stessa firma di `open-webui`, che ha mediana 138 MiB perché
muore prima di crescere. **Un tetto troppo stretto si legge come poco consumo.**

**I 12 campioni con `containers._error`** (`docker stats` in timeout a 30 s) non sono rumore
del campionatore: si raggruppano sui riavvii dei container. Il grappolo delle 17:52–17:54
coincide col riavvio di ES delle 17:52:05. Sono churn, e il campionatore ha fatto la cosa
giusta registrandoli come errore invece di produrre un totale parziale silenzioso.

## 4. Cosa questo run **non** dice

- **Non chiude T-SOAK-24h**: 7,38 h su 24.
- **Non attribuisce il merito** fra `mem_limit` (#21) e `jvm.config` (#22): cambiano insieme.
- **Non valida i tier**: ENV-W ha 256 GB, e il piano (§143) dice che le misure di RAM qui non
  sono trasferibili. Ma **questo confronto non ne ha bisogno**: è un differenziale sulla stessa
  macchina, con la sola configurazione a cambiare.
- **Non dice nulla di `open-webui`** in questo run: contaminato per costruzione.

## 5. Costo della sessione

**Non misurabile** (sessione bridge).
