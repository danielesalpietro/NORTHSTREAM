# NORTHSTREAM - Recommended tier
#
# Recommended hardware for this tier:
#   RAM:    32 GB
#   CPU:    8 cores / vCPUs
#   VRAM:   not required (CPU inference), but any GPU speeds up chat
#   GPU:    optional, e.g. NVIDIA GTX 1660 / RTX 3050 (4-6 GB VRAM) or better
#
# Use together with examples/recommended/.wslconfig (Windows/WSL2) for
# consistent resource limits.
#
# Copy this file to the repository root as ".env" (docker compose loads it
# automatically) to select the models used by the Stream Context Agent.
OLLAMA_CHAT_MODEL=granite4:1b
OLLAMA_EMBED_MODEL=granite-embedding:30m
