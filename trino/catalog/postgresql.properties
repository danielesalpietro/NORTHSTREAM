# Trino catalog for the operational Postgres source (finding P-2, issue #22).
#
# Catalog name is derived from this filename: "postgresql" — pinned by the
# T0.7 progression test and by docs/piano_ricovero.md §4.1, which both query
# postgresql.public.orders. Renaming this file changes the catalog name and
# breaks T0.7.
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/sales
connection-user=demo
connection-password=demo
